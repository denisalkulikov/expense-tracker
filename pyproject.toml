[project]
name = "expense-tracker"
version = "1.0.0"
description = "Expense Tracker with Supabase Auth"
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.5",
    "plotly>=6.9.0",
    "streamlit>=1.61.1",
    "supabase>=2.31.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.3.0"]
